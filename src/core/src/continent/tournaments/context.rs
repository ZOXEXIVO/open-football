pub struct TournamentContext {}

impl Default for TournamentContext {
    fn default() -> Self {
        Self::new()
    }
}

impl TournamentContext {
    pub fn new() -> Self {
        TournamentContext {}
    }
}
